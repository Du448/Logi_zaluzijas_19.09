# Copy this file to .env.local and fill in your SMTP credentials
# Example for Gmail/Workspace (use an app password), or your SMTP provider

# SMTP server configuration
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Optional TLS flags (use ONLY for local development if you face self-signed cert errors)
# Do NOT enable these in production.
# Allow self-signed certificates in the TLS chain
# SMTP_ALLOW_SELF_SIGNED=false
# Ignore upgrading to TLS entirely (rarely needed; for legacy servers)
# SMTP_IGNORE_TLS=false
# Enforce STARTTLS when using port 587
# SMTP_REQUIRE_TLS=false

# Alternative (STARTTLS) example instead of SMTPS (465):
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_REQUIRE_TLS=true

# Contact routing
CONTACT_TO_EMAIL=info@vestalux.lv
# Optional: the visible From header (defaults to SMTP_USER)
CONTACT_FROM_EMAIL="no-reply@vestalux.lv"

# Google reCAPTCHA v2 Checkbox
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=""
RECAPTCHA_SECRET_KEY=""
